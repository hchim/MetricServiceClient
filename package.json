{
  "name": "metricsclient",
  "version": "0.1.0",
  "description": "Metric service client for node.js",
  "main": "dist/index.js",
  "scripts": {
    "test": "npm run build && mocha \"dist/test/*.js\"",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./package-lock.json,README.md --copy-files"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:hchim/MetricServiceClient.git"
  },
  "keywords": [
    "metric"
  ],
  "author": "Hui Chen",
  "license": "MIT",
  "dependencies": {
    "mocha": "^5.2.0",
    "chai": "^4.1.2",
    "request": "^2.81.1",
    "string-format-js": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  }
}
